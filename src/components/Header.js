import React from "react";

const Header = () => {
  return (
    <div style={{ display: "flex", justifyContent: "center" }}>
      <h3 style={{ fontSize: "2rem", color: "gray" }}>TODO APP</h3>
    </div>
  );
};

export default Header;
